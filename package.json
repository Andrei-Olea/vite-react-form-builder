{
  "name": "vite-react-form-builder",
  "version": "1.0.0",
  "type": "module",
  "description": "A Vite + React form builder starter template",
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "dev": "vite",
    "dev:smart": "bash dev.sh",
    "build": "tsc && vite build && pnpm run postbuild",
    "postbuild": "ln -sf dist/assets assets 2>/dev/null || true && ln -sf dist/fonts fonts 2>/dev/null || true && ln -sf dist/img img 2>/dev/null || true && echo '✓ Build complete - symlinks created for DDEV (nginx) | .htaccess ready for Apache'",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
